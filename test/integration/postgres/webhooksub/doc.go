// Package webhooksub holds the PostgreSQL integration suite for
// internal/repo/webhooksub. Untagged so `go vet ./...` can enumerate the
// package; the tests themselves carry the integration build tag.
package webhooksub
