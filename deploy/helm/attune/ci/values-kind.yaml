profile: dev

postgres:
  embedded:
    auth:
      password: attune-dev
    persistence:
      enabled: false

# Leave observability.environment unset so the chart default can follow profile.
resources:
  requests:
    cpu: 50m
    memory: 128Mi

helmTest:
  enabled: true
